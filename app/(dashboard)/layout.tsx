import React from "react";

const Layout = ({ children }: { children: React.ReactNode }) => {
  return (
    <div>
      <h2>Dashboard NavBar</h2>
      Dashboard Layout 1<h2>Dashboard Footer</h2>
    </div>
  );
};

export default Layout;
